Simple OpenAi http client to use for automatic parsing of structured outputs into structs. 

Although some OpenAi clients already exist, this is the only one that parses directly into the desired output.
